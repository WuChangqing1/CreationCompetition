# MPDD-AVG 2026 数据集全面分析

> 分析日期：2026-06-04
> 数据路径：`test/Elder/` + `test/Young/`
> 基线代码：`test/MPDD-AVG-2026-main/`

---

## 一、总体概览

| 维度 | Elder (Track1) | Young (Track2) | 合计 |
|---|---|---|---|
| 被试人数 | 87 | 88 | **175** |
| 年龄组 | 老年 | 青年 | — |
| 训练样本（split=train） | 87 (100%) | 88 (100%) | 175 |
| 预定义验证集 | **无** | **无** | — |
| 预定义测试集 | **无** | **无** | — |
| 2026官方测试集 | **空目录** | **空目录** | — |

### 核心问题

**这是典型的极小样本医疗数据集。** 175 个被试，无预定义验证/测试划分，官方测试集未发布。模型训练完全依赖 `train_val_split.py` 的 9:1 随机划分（Elder: 78 train / 9 val, Young: 79 train / 9 val）。

---

## 二、目录结构

```
test/
├── Elder/                            # Track1 老年组 (87人)
│   ├── split_labels_train.csv        # 标签文件
│   ├── descriptions_embeddings_with_ids.npy  # 人格特征 (110条)
│   ├── personalized_descriptions.csv # 人格描述原文
│   ├── Audio/train/
│   │   ├── mfcc/          (87人, 4片段/人, 64维)
│   │   ├── opensmile/     (87人, 4片段/人, 65维)
│   │   └── wav2vec2/      (87人, 4片段/人, 768维)
│   ├── Video/train/
│   │   ├── densenet/      (87人, 4片段/人, 1000维)
│   │   ├── resnet/        (87人, 4片段/人, 1000维)
│   │   └── openface/      (87人, 4片段/人, 710维, flat .npy)
│   └── IMU/train/         (87人, 1文件/人, 12维步态)
│
├── Young/                            # Track2 青年组 (88人)
│   ├── split_labels_train.csv        # 标签文件
│   ├── descriptions.csv              # 人格描述原文
│   ├── descriptions_embeddings_with_ids.npy  # 人格特征 (110条)
│   ├── Audio/train/
│   │   ├── mfcc64/        (88人, 3片段/人, 64维)  ← 注意: mfcc64 非 mfcc
│   │   ├── opensmile/     (88人, 3片段/人, 65维)
│   │   └── wav2vec2/      (88人, 3片段/人, 1024维) ← 注意: 1024维非768维
│   ├── Video/train/
│   │   ├── densenet/      (88人, 3片段/人, 1000维)
│   │   ├── resnet/        (88人, 3片段/人, 1000维)
│   │   └── openface/      (88人, 3片段/人, 710维, 嵌套结构)
│   └── IMU/train/         (88人, 1文件/人, 12维步态)
│
└── MPDD-AVG-2026-main/
    └── MPDD-AVG2026/MPDD-AVG2026-test/   ← 空目录，测试集未发布
```

---

## 三、标签分布

### 3.1 二分类 (label2: 0=非抑郁, 1=抑郁)

| 数据集 | 总数 | class 0 | class 1 | 不平衡比 |
|---|---|---|---|---|
| Elder | 87 | 59 (67.8%) | 28 (32.2%) | **2.1:1** |
| Young | 88 | 45 (51.1%) | 43 (48.9%) | **1.05:1** (均衡) |

### 3.2 三分类 (label3: 0=无, 1=轻度, 2=中重度)

| 数据集 | 总数 | class 0 | class 1 | class 2 | 不平衡比 |
|---|---|---|---|---|---|
| Elder | 87 | 59 (67.8%) | 17 (19.5%) | 11 (12.6%) | **5.4:1.5:1** |
| Young | 88 | 45 (51.1%) | 33 (37.5%) | 10 (11.4%) | **4.5:3.3:1** |

### 3.3 PHQ-9 回归

| 数据集 | 范围 | 均值 | 中位数 | 分布特征 |
|---|---|---|---|---|
| Elder | 0 – 23 | 4.45 | 3.0 | 右偏，多数低分 |
| Young | 0 – 17 | 4.82 | 4.0 | 较均匀 |

### 分析

- **Elder binary 中度不均衡** (2.1:1)，ternary 严重不均衡 (5.4:1.5:1)，class 2 仅 11 人
- **Young binary 接近完美均衡** (1.05:1)，ternary 仍不均衡但好于 Elder
- 三分类 class 2 在两组都极度稀缺（Elder 11 人 / Young 10 人），**三分类在当前数据量下基本不可行**
- 9 人验证集中 class 2 预期仅 ~1 人，验证指标几乎无意义

---

## 四、特征维度详解

### 4.1 音频特征

| 特征 | Elder 维度 | Young 维度 | Elder 文件/人 | Young 文件/人 | 说明 |
|---|---|---|---|---|---|
| mfcc / mfcc64 | (T, 64) | (T, 64) | 4 | 3 | Young 命名不同(mfcc64) |
| opensmile | (T, 65) | (T, 65) | 4 | 3 | eGeMAPS 特征集 |
| wav2vec2 | (T, **768**) | (T, **1024**) | 4 | 3 | **维度不一致！** |

**wav2vec2 维度不一致是严重问题。** Elder=768, Young=1024。如果想把 Elder+Young 联合训练，wav2vec2 特征将无法直接拼接。

### 4.2 视频特征

| 特征 | 维度 | Elder 文件/人 | Young 文件/人 | 文件结构 |
|---|---|---|---|---|
| densenet | (T, 1000) | 4 | 3 | flat .npy |
| resnet | (T, 1000) | 4 | 3 | flat .npy |
| openface | (T, 710) | 4 | 3 | **Elder flat / Young 嵌套** |

Young OpenFace 结构特殊：`{subject}/event_N/event_N_all.npy`（嵌套），而 Elder 是 `{subject}/V_N.npy`（flat）。基线代码通过 `_discover_young_pair_files()` 处理，已验证兼容。

### 4.3 步态特征 (IMU)

| 特征 | 维度 | 文件/人 | 大小范围 |
|---|---|---|---|
| gait | (T, **12**) | 1 | 55KB – 297KB (Elder), 158KB – 202KB (Young) |

12 维步态特征含左右脚踝和腰部 IMU 数据，经过 PCA/特征提取压缩。**维度极低，信息量有限。**

### 4.4 人格特征 (Personality)

| 数据集 | 总条数 | 维度 | 有效被试覆盖 |
|---|---|---|---|
| Elder | 110 | **1024** (RoBERTa) | 87/110 (79%) |
| Young | 110 | **1024** (RoBERTa) | 88/110 (80%) |

人格特征由 RoBERTa 模型编码个性化描述文本生成。110 条中包含 23 条（Elder）/ 22 条（Young）未被使用的 embedding（可能是预留或过滤掉的被试）。

### 4.5 子赛道特征组合

| 子赛道 | 输入维度 (Elder) | 输入维度 (Young) | 说明 |
|---|---|---|---|
| **G+P** | 12 + 1024 = **1036** | 12 + 1024 = **1036** | 步态+人格 |
| **A-V+P** | 65~88 + 710~1000 + 1024 ≈ **1800~2100** | 65 + 710~1000 + 1024 ≈ **1800~2100** | 音+视+人格 |
| **A-V-G+P** | +12 ≈ **2100+** | +12 ≈ **2100+** | 全特征 |

**78 个训练样本 vs 2100 维输入 → 维度灾难。** 这是 A-V+P 性能不如 G+P 的根本原因。

---

## 五、数据质量

### 5.1 不完整样本

| 问题 | 影响范围 | 详情 |
|---|---|---|
| **Elder 5人仅有2片段** | ID: 30, 39, 50, 54, 87 | 其他人 4 片段，这 5 人仅 2 片段（音频+视频均缺） |
| **Young OpenFace 目录格式不同** | 全部 88 人 | 嵌套 event_N/ 结构，需特殊解析代码 |

### 5.2 NaN/Inf 检测

对全部模态采样检测（每模态 5-20 文件），**未发现任何 NaN 或 Inf 值**。数据质量总体良好。

### 5.3 空文件检测

**所有 .npy 文件均非空。** 之前记录的 K001/K002（Young test 空文件）在本数据目录中不存在——当前仅有 train 数据。

### 5.4 被试 ID 连续性

- Elder: ID 范围 2–123，**36 个 ID 缺失**（如 1,6,8,11,13 等）
- Young: ID 范围 2–109，**21 个 ID 缺失**（如 1,5,7,13,15 等）
- ID 不连续意味着**不能依赖 ID 连续性做索引**

---

## 六、训练/验证/测试划分

### 6.1 当前状态

```
split_labels_train.csv:
  - split 列: 100% = "train"
  - 无预分割的 val 或 test 子集
  - Audio/Video/IMU 目录下仅有 train/ 子目录，无 test/ 子目录
```

### 6.2 划分机制

基线代码 `train_val_split.py` 执行 `train_test_split(val_ratio=0.1, stratify=labels)`：

| 数据集 | 训练样本 | 验证样本 | 测试样本 |
|---|---|---|---|
| Elder | 78 | **9** | 无 |
| Young | 79 | **9** | 无 |

### 6.3 验证集问题

**9 个验证样本是全部问题的根源：**

- 二分类 (9人): 预测错 1 人 → F1 波动 ~0.2，kappa 波动 ~0.3
- 三分类 (9人): class 2 预期仅 ~1 人，几乎无法评估
- 回归 (9人): CCC 极不稳定，受单一样本影响极大
- 每次训练的 split 不同（seed 在 split 之后设置），**结果不可完全复现**

---

## 七、跨数据集差异总结

| 差异点 | Elder | Young | 影响 |
|---|---|---|---|
| 片段数 | 4 (5人仅2) | 3 | 配对数量影响 multi-pair 编码 |
| wav2vec2 维度 | 768 | **1024** | **联合训练时维度不兼容** |
| mfcc 命名 | mfcc | mfcc64 | 已兼容 (dataset.py 别名处理) |
| OpenFace 结构 | flat V_N.npy | 嵌套 event_N/ | 已兼容 (_discover_young_pair_files) |
| binary 均衡性 | 2.1:1 (不均衡) | 1.05:1 (均衡) | Elder 需要 class_weights |
| ternary 均衡性 | 5.4:1.5:1 (严重) | 4.5:3.3:1 (中度) | 两组三分类都困难 |

---

## 八、诊断结论

### 8.1 为什么模型难以训练

```
                   训练样本数        78
特征维度 (A-V+P)    ~2100
特征/样本比         27:1  ← 维度灾难
验证样本            9      ← 指标极度不稳定
类别不均衡          2.1:1  ← 少数类样本更少
```

### 8.2 为什么 G+P 比 A-V+P 表现好

| | G+P | A-V+P |
|---|---|---|
| 输入维度 | 1036 | ~2100 |
| 主要信号源 | 人格 (1024d RoBERTa) | 人格 + 音频 + 视频 |
| 有效信号密度 | **高**（人格特征信息密度大） | **低**（音频/视频维度高但噪声大） |
| 模型学习难度 | 低 | 高 |

人格特征是 RoBERTa 从文本描述中提取的语义向量，本身就富含抑郁症相关信息。音频/视频特征虽然维度高，但对于 78 个样本来说噪声远大于信号。

### 8.3 为什么三分类完全失败

- Elder: class 2 仅 11 人（训练集约 10 人，验证集约 1 人）
- 模型基本无法学到 class 2 的特征模式
- 验证集中的 class 2 样本（1人）完全无法评估模型能力

### 8.4 为什么相同参数不同运行结果差异大

- seed 在 `create_train_val_split()` 之后才设置 → 每次 split 不同
- 9 人验证集 → F1 波动可达 0.2-0.3
- G+P binary F1 真实范围约 0.55-0.88（参见本次训练中 epoch 71=0.88 vs epoch 77=0.55）

---

## 九、改进建议

1. **交叉验证替代单次 split**：用 5-fold CV 替代 9:1 单次划分，验证指标更稳定
2. **Elder+Young 联合训练（二分类）**：175 样本 vs 87，样本量翻倍；但需解决 wav2vec2 维度不一致问题
3. **仅对人格特征做三分类**：~1024 维 RoBERTa 特征 + 175 样本，可能勉强可行
4. **专注二分类+回归**：三分类在当前数据规模下信息量不足，建议降低优先级
5. **fix seed 顺序**：将 `setup_seed()` 移到 `create_train_val_split()` 之前，确保可复现
6. **数据增强**：对音频片段做时间偏移/加噪，利用多片段特性扩充训练集
7. **特征降维**：对 1000d 视频特征做 PCA 降至 128-256d，缓解维度灾难
8. **修复 elder 5 人不完整**：对于仅有 2 片段的被试，考虑填充或降权
